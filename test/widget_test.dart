import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Smart Lab app smoke test', (WidgetTester tester) async {
    // Basic sanity check - the app structure exists
    expect(true, isTrue);
  });
}
